// pega o elemento com a id "resultado"
const elemento = document.getElementById("resultado")

// cria uma constante com a mensagem "Hello, World!"
const mensagem = "Hello, World!";

// altera o conteúdo do elemento para a mensagem
elemento.textContent = mensagem;