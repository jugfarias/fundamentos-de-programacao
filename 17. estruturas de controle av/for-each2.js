// exemplo simples de for each

// cria um array
const frutas = ["banana", "maçã", "uva", "pera", "melancia"];

// para cada fruta no array de frutas, imprime a fruta
frutas.forEach(function(fruta) {
    console.log(fruta);
});